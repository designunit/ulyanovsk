export const Caption: React.FC = props => (
    <span style={{
        fontSize: '1em',
        color: '#091133',
        fontWeight: 800,
        display: 'inline-block',
        marginBottom: 8,
    }}>{props.children}</span>
)